# OpenDrift — Docker dev environment variables
# Copy this file to .env and fill in your key
# This file is safe to commit — .env is gitignored

# OpenCode API key (used for LLM access via OpenCode)
OPENCODE_API_KEY=
